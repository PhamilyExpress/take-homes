This code was written in elixir. To install elixir run:

brew install elixir


To start the script:

1. cd to file directory

2. Run this command:

mix run -e "FrameTakehome.read_file('filename.txt')"